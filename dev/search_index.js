var documenterSearchIndex = {"docs":
[{"location":"blog/#Blog","page":"Blog","title":"Blog","text":"Pages = [\n    \"_posts/2025-11-11_rings.md\",\n    \"_posts/2025-09-14-space_system.md\",\n]\nDepth = 3","category":"section"},{"location":"_posts/2025-09-14-space_system/#Mavi's-Space-System","page":"Mavi's Space System","title":"Mavi's Space System","text":"Here's a quick post explain how Mavi deals with systems in spaces with different geometries and border conditions.","category":"section"},{"location":"_posts/2025-09-14-space_system/#Defining-a-Space","page":"Mavi's Space System","title":"Defining a Space","text":"A Space is defined by its geometry and how the borders (or walls) should behave, so there is a clear separation between geometry and wall behavior. Geometries are types derived from GeometryCfg, while walls are types derived from WallType. A space is simply the combination of a GeometryCfg and WallType, in code, this look like this\n\nabstract type GeometryCfg end\nabstract type WallType end\n\nstruct SpaceCfg{W<:WallType, G<:GeometryCfg} \n    wall_type::W\n    geometry_cfg::G\nend\n\nwhen creating a system, one needs to provide some SpaceCfg.\n\nFor instance, we can create a rectangular geometry (with an arbitrary number of dimension) and periodic walls in the fallowing way\n\nusing StaticArrays # Needed to use SVector\n\n# \"Rectangle\" with N dimensions using the number type T\nstruct RectangleCfg{N, T<:Number} <: GeometryCfg\n    bottom_left::SVector{N, T}\n    size::SVector{N, T} # size[i] is the rectangle's length in the i'th dimension\nend\n\nstruct PeriodicWalls <: WallType end  \n\nthe actual code dealing with this kind of space should be in a specialized function, see next section.","category":"section"},{"location":"_posts/2025-09-14-space_system/#Dealing-with-Walls-Behavior","page":"Mavi's Space System","title":"Dealing with Walls Behavior","text":"Walls collisions should be resolved in the function walls!(system, space_cfg::SpaceCfg), this function should dispatch on every kind of space there exists, for instance, with a rectangular geometry and periodic walls, the following method should be created.\n\nfunction walls!(system, space_cfg::SpaceCfg{PeriodicWalls, RectangleCfg{N, T}}) where {N, T}\n    # code to resolve walls collisions here\n\nwalls! must modify particles positions when necessary. Usually, walls! should be called after the update of positions by the equations of motion.","category":"section"},{"location":"_posts/2025-09-14-space_system/#Calculating-Distances","page":"Mavi's Space System","title":"Calculating Distances","text":"Sometimes we need to know the information about the space to calculate distances (such as periodic boundaries with rectangle geometry), so there exists the function calc_diff(r1, r2, space_cfg), which calculates the difference between positions r1 and r2, and should dispatch on space_cfg. For instance,\n\n# Default behavior\nfunction calc_diff(r1, r2, space_cfg)\n    @inbounds dr = r1 - r2\n    return dr\nend\n\n# Periodic boundaries with rectangle geometry\nfunction calc_diff(r1, r2, space_cfg::SpaceCfg{PeriodicWalls, G}) where G <: RectangleCfg\n    size_vec = space_cfg.geometry_cfg.size\n    dr = r1 - r2\n    dr = dr - (abs.(dr) .> (size_vec / 2)) .* copysign.(size_vec, dr)\n    return dr\nend","category":"section"},{"location":"_posts/2025-09-14-space_system/#Using-Multiple-Spaces","page":"Mavi's Space System","title":"Using Multiple Spaces","text":"It is possible to use multiple spaces, this is useful to insert obstacles in the simulation or create complex geometries using simple shapes. This functionality is accomplished by defining the types\n\nstruct ManyGeometries{G <: Tuple} <: GeometryCfg\n    list::G\nend\n\nstruct ManyWalls{W <: Tuple} <: WallType \n    list::W\nend  \n\nthen, the walls behavior can simply be done like this\n\nfunction walls!(system::System, space_cfg::SpaceCfg{W, G}) where {W <: ManyWalls, G <: ManyGeometries}\n    for (wall_cfg, geom_cfg) in zip(space_cfg.wall_type.list, space_cfg.geometry_cfg.list)\n        walls!(system, SpaceCfg(wall_cfg, geom_cfg))\n    end\nend\n\nto make life easier for users, there exists a constructor for SpaceCfg to create composite spaces\n\n# `spaces_cfg_list` must be a list of pairs of type (WallType, GeometryCfg).\nfunction SpaceCfg(spaces_cfg_list)\n    walls = []\n    geometries = []\n\n    for (w, g) in spaces_cfg_list\n        push!(walls, w)\n        push!(geometries, g)\n    end\n\n    SpaceCfg(\n        wall_type=ManyWalls(Tuple(walls)),\n        geometry_cfg=ManyGeometries(Tuple(geometries)),\n    )\nend\n\nin fact, users should always use this constructor, because it ensures that the list of geometries and walls are in the correct order.\n\nOBS: Some functions in Mavi needs to know information about the space where particles are, therefore, when using multiple spaces, this information becomes not defied (because some spaces might just be obstacles). To mitigate this, the first space, in a space with multiple spaces, is considered as the \"main\" one.","category":"section"},{"location":"manual/#Manual","page":"Manual","title":"Manual","text":"","category":"section"},{"location":"manual/#Installation","page":"Manual","title":"Installation","text":"The easiest way to install Mavi is:\n\nOpen the Julia REPL and enter package manager mode (by pressing ] at the empty REPL prompt)\nExecute the command:\n\npkg> add https://github.com/marcos1561/Mavi.jl.git ","category":"section"},{"location":"manual/#Quick-Start","page":"Manual","title":"Quick Start","text":"Let's create a gas of particles. The first thing we need to do is create the system's state. Particles' positions will be initialized at the vertices of a rectangular grid with random velocities. To initialize the positions, we need to know the particle radius, which is defined by the interaction potential between them (in this example we will use a Lennard-Jones potential). The particle radius can be accessed via the particle_radius() function:  \n\nusing Mavi.Configs\nusing Mavi.States\nusing Mavi.InitStates\n\nfunction main()\n    # Init state configs\n    num_p_x = 10 # Number of particles in the x direction\n    num_p_y = 10 # Number of particles in the y direction\n    offset = 0.4 # Space between particles\n\n    num_p = num_p_x * num_p_y\n    dynamic_cfg = LenJonesCfg(sigma=1, epsilon=1) # Interaction potential between particles\n    radius = particle_radius(dynamic_cfg)\n\n    # rectangular_grid returns positions in a rectangular grid (`pos`) and\n    # a rectangular geometry configuration that contains all positions (geometry_cfg)\n    pos, geometry_cfg = rectangular_grid(num_p_x, num_p_y, offset, radius)\n    \n    # Creating a state compatible with Newton's Second Law\n    state = SecondLawState(\n        pos=pos,\n        vel=random_vel(num_p, 1/5),\n    )\nend\n\nNext, we need to specify the space where the particles live. Luckily, rectangular_grid() already gives us a geometry configuration that contains all the positions created, we only need to specify how the walls behave (we will use rigid walls)\n\nusing Mavi.Configs\nusing Mavi.States\nusing Mavi.InitStates\n\nfunction main()\n    # Init state configs\n    num_p_x = 25 # Number of particles in the x direction\n    num_p_y = 25 # Number of particles in the y direction\n    offset = 0.4 # Space between particles\n\n    num_p = num_p_x * num_p_y\n    dynamic_cfg = LenJonesCfg(sigma=1, epsilon=1) # Interaction potential between particles\n    radius = particle_radius(dynamic_cfg)\n\n    # rectangular_grid returns positions in a rectangular grid (`pos`) and\n    # a rectangular geometry configuration that contains all positions (geometry_cfg)\n    pos, geometry_cfg = rectangular_grid(num_p_x, num_p_y, offset, radius)\n    \n    # Creating a state compatible with Newton's Second Law\n    state = SecondLawState(\n        pos=pos,\n        vel=random_vel(num_p, 1/5),\n    )\n\n    # Creating space configurations\n    space_cfg = SpaceCfg(\n        wall_type=RigidWalls(),\n        geometry_cfg=geometry_cfg,\n    )\nend\n\nNow we can put it all together in a System and animate! \n\nusing Mavi\nusing Mavi.Configs\nusing Mavi.States\nusing Mavi.InitStates\n\nusing Mavi.Visualization\n\nfunction main()\n    # Init state configs\n    num_p_x = 10 # Number of particles in the x direction\n    num_p_y = 10 # Number of particles in the y direction\n    offset = 0.4 # Space between particles\n\n    num_p = num_p_x * num_p_y\n    dynamic_cfg = LenJonesCfg(sigma=1, epsilon=1) # Interaction potential between particles\n    radius = particle_radius(dynamic_cfg)\n\n    # rectangular_grid returns positions in a rectangular grid (`pos`) and\n    # a rectangular geometry configuration that contains all positions (geometry_cfg)\n    pos, geometry_cfg = rectangular_grid(num_p_x, num_p_y, offset, radius)\n    \n    # Creating a state compatible with Newton's Second Law\n    state = SecondLawState(\n        pos=pos,\n        vel=random_vel(num_p, 1/5),\n    )\n\n    # Creating space configurations\n    space_cfg = SpaceCfg(\n        wall_type=RigidWalls(),\n        geometry_cfg=geometry_cfg,\n    )\n\n    system = System(\n        state=state, \n        space_cfg=space_cfg,\n        dynamic_cfg=dynamic_cfg,\n        int_cfg=IntCfg(dt=0.01), # Configurations related to how the system is integrated, such as the time step (dt)\n    )\n\n    animate(system)\nend\n\nAfter running main(), you should see this:\n\n(Image: Quick Start simulation snapshot)","category":"section"},{"location":"manual/#Mavi-Philosophy","page":"Manual","title":"Mavi Philosophy","text":"Every system of particles is represented by the same struct System, whose main members are:\n\nstate: Data representing the particles' states.\nspace_cfg: Information about the geometry of the space (including boundary behavior) where the particles are located.\ndynamic_cfg: Configuration of the dynamics between particles.\nint_cfg: Configuration of how to integrate the equations of motion.\n\nIn order to make things work, all these members' types are parametric, so Julia's multiple dispatch mechanism can shine.\n\nA System should be integrated by a function that executes one time step at a time. Usually, a step function looks like this:\n\nfunction step!(system)\n    clean_forces!(system) # Sets forces array to zero\n    calc_forces!(system) # Calculates all the forces\n    update!(system)      # Integrates equations of motion\n    walls!(system)       # Resolves wall collisions\nend\n\nMavi already has some step functions (see integration.jl), but users are free to create their own.","category":"section"},{"location":"manual/#About-System-Members","page":"Manual","title":"About System Members","text":"In this section we will talk about the main system's members: what they represent and how to create them.  ","category":"section"},{"location":"manual/#State","page":"Manual","title":"State","text":"The state of a system is defined by the equations that govern its dynamics. For example, an ordinary, linear, second-order differential equation in x(t) requires that two variables be stored in memory (the variable x itself and its derivative dot x). States must be subtypes of State{T} (see states.jl for examples), where T is the number type (Int32, Float64, etc). Currently, two states are implemented in the core Mavi.States module:\n\nSecondLawState: has pos and vel as members, both of which must be 2 times N matrices or Vector{SVector}. This state is useful when the equation to be solved is Newton's second law.\nSelfPropelledState: a state related to a system of particles with overdamped dynamics and a self-alignment mechanism.\n\nThe following example creates a state with 3 particles using SecondLawState.\n\nusing Mavi.States\n\nstate = SecondLawState(\n    pos = [1 2 3; 1 2 3],\n    vel = [1.1 0.4 3; 1.2 2 -0.4],\n)","category":"section"},{"location":"manual/#SpaceCfg","page":"Manual","title":"SpaceCfg","text":"The space of a system is described by its geometric shape and how its boundaries behave:\n\nSpace geometry: These are structures that inherit from GeometryCfg and contain all the necessary information to describe the geometry of the space. Examples: RectangleCfg and CircleCfg.\n\nBoundary behavior: These are structures that inherit from WallType and do not necessarily need to have any fields; they simply indicate the boundary type. Examples: RigidWalls and PeriodicWalls.\n\nSpaceCfg encapsulates these two structures. See configs.jl for more details.\n\nExample: Configuring a rectangle with its bottom-left corner at the origin and with periodic boundaries:\n\nusing Mavi.Configs\n\nspace_cfg = SpaceCfg(\n    geometry_cfg=RectangleCfg(\n        length=5,\n        height=10,\n    ),\n    wall_type=PeriodicWalls(),\n)","category":"section"},{"location":"manual/#DynamicCfg","page":"Manual","title":"DynamicCfg","text":"This object should have configurations about the interactions between particles, such as potential parameters. Every instance is a subtype of DynamicCfg.\n\nAs examples, Mavi.Configs module has these potential structures:\n\nHarmonic Truncated Potential: HarmTruncCfg(ko, ro, ra)\nLennard-Jones Potential: LenJonesCfg(sigma, epsilon)\n\nDetails about parameters can be found in configs.jl.\n\nDynamicCfg is used to calculate forces, users should only implement a method for calc_interaction(i, j, dynamic_cfg, space_cfg) (inside module Mavi.Integration), where i and j are particle indices (more info in its documentation integration.jl).\n\nAlso, particles radius should be inferred from these configurations, so there exists a function particle_radius(dynamic_cfg). ","category":"section"},{"location":"manual/#IntCfg","page":"Manual","title":"IntCfg","text":"Configurations for how the equations of motion should be integrated. Every instance is a subtype of AbstractIntCfg. The default integration configuration inside Mavi.Configs is IntCfg, which has two main members:\n\ndt: Time step used in the integration.\nchunks_cfg: Configurations of the technique used to speed distance calculations: the system is divided into boxes, or chunks, and the interaction between particles is calculated only for neighboring boxes, greatly reducing the simulation time for short range interactions. If its value is nothing, then this technique is not used.\n\nThe following example creates a system with a circular space, using the Lennard-Jones potential and simple time integration:\n\nusing Mavi\nusing Mavi.Configs\n\nsystem = System(\n    state=state, # assuming the state has already been created\n    space_cfg=CircleCfg(radius=10), \n    dynamic_cfg=LenJonesCfg(sigma=2,epsilon=4),\n    int_cfg=IntCfg(dt=0.01),\n)","category":"section"},{"location":"manual/#About-the-step-function","page":"Manual","title":"About the step function","text":"The step function should evolve a System in one time step. The actions usually needed to perform a time step are: \n\ncalculate forces\nintegrate equations of motion\nresolve wall collisions    \n\nMavi already has a general system to compute pair interactions, some ready to use functions to integrate equations of motion (such as Newton's Second Law) and wall collisions resolvers, but users are free to create their own methods as specified below.","category":"section"},{"location":"manual/#How-to-use-custom-forces?","page":"Manual","title":"How to use custom forces?","text":"Forces are calculated based on DynamicCfg with the function calc_interaction(i, j, dynamic_cfg, system) (which is in the module Mavi.Integration and in the file integration.jl). So, one can use custom forces creating a new DynamicCfg and a method for calc_interaction\n\nExample: Let's create a constant radial force (with modulus force) applied only when the distance between particles is less than min_dist\n\nusing Mavi.Configs\nusing Mavi.Integration\n\n# New DynamicCfg struct\nstruct RadialForce <: DynamicCfg\n    force::Float64\n    min_dist::Float64\nend\n\n# Creating new method used in the forces calculation.\nfunction Integration.calc_interaction(i, j, dynamic_cfg::RadialForce, system)\n    pos = system.state.pos\n    dr = calc_diff(pos[i], pos[j], system.space_cfg)\n    dist = sqrt(sum(dr.^2))\n\n    force = dynamic_cfg.force\n    min_dist = dynamic_cfg.min_dist\n\n    if dist > min_dist\n        return zero(eltype(pos))\n    end\n\n    return force * dr / dist\nend\n\n# This method is used to draw the particles\nConfigs.particle_radius(dynamic_cfg::DynamicCfg) = dynamic_cfg.min_dist/2\n\nAfter that, a System can be created as usual, with dynamic_cfg set to RadialForce. A complete example can be found here custom_force.jl.\n\nNote: The function that computes all the pairwise forces is calc_forces!(), it is inside the module Mavi.Integration and in the file integration.jl.","category":"section"},{"location":"manual/#How-to-use-different-equations-of-motion?","page":"Manual","title":"How to use different equations of motion?","text":"Just create your own step function with the appropriate equations' of motion. Remember, you can reuse the general method to compute pairwise forces (calc_forces!()) and wall collision detections, thus just focusing on the equations of motion.\n\nMavi already has functions to integrate some equations of motion, such as:\n\nupdate_verlet!: Newton Second Law integrated using the verlet method. \nupdate_rtp!: Run-and-Tumble particles.\nupdate_szabo!: Szabo particles, which follow the equations of motion introduced in the paper \"Phase transition in the collective migration of tissue cells: experiment and model\" by Szabó, B., et al. (Physical Review E, 2006).","category":"section"},{"location":"manual/#How-do-wall-collisions-work?","page":"Manual","title":"How do wall collisions work?","text":"The method walls!(system, space_cfg) (inside module Mavi.Integration) is responsible for resolving wall collisions. It dispatches on space_cfg, therefore, for instance, to implement rigid walls in a rectangular geometry (this method is already implemented on Mavi.jl), one should create the method\n\nfunction walls!(system, space_cfg::SpaceCfg{RigidWalls, RectangleCfg})\n    ...\nend\n\nwalls! should directly change the state in system, resolving the collisions.\n\nNote: See more about how Mavi deals with wall collisions in the blog post Mavi's Space System.","category":"section"},{"location":"manual/#Physical-Quantities","page":"Manual","title":"Physical Quantities","text":"The Quantities module contains functions that return physical quantities of interest. Currently, two of them are defined:\n\nKinetic energy:     The system's kinetic energy is calculated by the function kinetic_energy(state), which takes the system's state as a parameter. Since it depends only on the velocities, it is independent of the chosen dynamic configuration.\nPotential energy:     The system's potential energy is calculated by the function potential_energy(system, dynamic_cfg), which takes the system and the dynamic configuration as arguments, since it depends on the chosen interaction between particles.     The user should ensure that the correct dispatch for the potential_energy() function is defined for any custom dynamic configuration.\n\nThe example print_energy.jl uses these functions.","category":"section"},{"location":"manual/#Running-Experiments","page":"Manual","title":"Running Experiments","text":"To collect data with simulations, we need to run an experiment. To create an experiment, one needs a system and a Collector (object that collects data while the simulation is running). Let's demonstrate how to run an experiment in Mavi:\n\nGiven we have a function create_system (which creates a system), the first thing to do is to create the experiment configurations\n\nusing Mavi.Experiments\n\nsystem = create_system()\n\nexperiment_cfg = ExperimentCfg(\n    tf=10, # Final simulation time\n    root=\"experiment_data\", # Where the experiment data will be saved\n)\n\nnext, we need to create the collector configurations, it's possible to use existing ones or create a custom collector, let's use the DelayedCol, which always have the system state some time in the past (this collector is useful to have the state system just before something bad happens, allowing the coder to investigate what is happening)\n\nusing Mavi.Experiments\n\nsystem = create_system()\n\nexperiment_cfg = ExperimentCfg(\n    tf=10, # Final simulation time\n    root=\"experiment_data\", # Where the experiment data will be saved\n)\n\ncollector_cfg = DelayedCfg(\n    delay_time=4, # How far in the past the state is\n)\n\nnow we can create an experiment and run it\n\nusing Mavi.Experiments\n\nsystem = create_system()\n\nexperiment_cfg = ExperimentCfg(\n    tf=10, # Final simulation time\n    root=\"experiment_data\", # Where the experiment data will be saved\n)\n\ncollector_cfg = DelayedCfg(\n    delay_time=4, # How far in the past the state is\n)\n\nexperiment = Experiment(experiment_cfg, collector_cfg, system)\n\nrun_experiment(experiment)\n\nall data collected by the Collector and other things will be inside in the root path, specified when creating ExperimentCfg. Here's a list (not complete) of what is saved in an experiment:\n\ndata: data collected by the Collector.\nfinal_state: system state just after the experiment finished.\nconfigs.json: system configurations used.\nexperiment_configs.json: experiment configurations used.","category":"section"},{"location":"manual/#Using-checkpoints","page":"Manual","title":"Using checkpoints","text":"If your experiment takes a lot of time, it's a good ideia to run the experiment with checkpoints, in that way, an experiment can be resumed from a checkpoint if something bad happens. To do that, simply pass a CheckpointCfg to ExperimentCfg\n\nexperiment_cfg = ExperimentCfg(\n    tf=10,\n    root=\"experiment_data\",\n    checkpoint_cfg=CheckpointCfg(delta_time=2), # Creates a checkpoint every two seconds (in simulation time)\n)\n\nand here is how to resume an experiment from a checkpoint\n\nusing Mavi.Experiment\n\nexperiment = load_experiment(\"path-to-experiment\")\nrun_experiment(experiment)","category":"section"},{"location":"manual/#Visual-Interface","page":"Manual","title":"Visual Interface","text":"Mavi has a visual interface (built entirely with Makie) whose purpose is to serve as a visual debugging tool for the system being explored. The UI structure essentially has two main elements:\n\nA plot where the system's particles are rendered in real time.\nA panel showing some information and buttons to control the animation.\n\n<img src=\"docs/images/ui_components.png\" alt=\"Componentes da UI\" width=\"500\"/>\n\n(Image: UI components)","category":"section"},{"location":"manual/#Animating-the-system","page":"Manual","title":"Animating the system","text":"To animate a system, simply call the function animate inside the module Mavi.Visualization\n\nusing Mavi.Visualization\n\n# Creating the system\nsystem = ...\n\nanimate(system)\n\nand if you have a custom step function called my_step!\n\nusing Mavi.Visualization\n\n# Creating the system\nsystem = ...\n\nanimate(system, step_func=my_step!)\n\nIt is possible to configure aspects of the animation by passing an instance of AnimationCfg to animate. The following example animates the system with the FPS set to 30, executing 15 time steps per animation frame:\n\nusing Mavi.Visualization\n\n# Creating the system\nsystem = ...\n\nanim_cfg = AnimationCfg(\n    fps=30,\n    num_steps_per_frame=15,\n)\n\nanimate(system, anim_cfg)","category":"section"},{"location":"manual/#Extending-the-Information-Panel","page":"Manual","title":"Extending the Information Panel","text":"It is possible to inject custom information into the information panel. This is done by setting the custom_items field of DefaultInfoUICfg, which in turn is a field of AnimationCfg. custom_items is a function that should return the additional information to be displayed in the information panel. For more details about its signature, see the documentation in info_ui.jl.\n\nThe following example uses a system already defined in Mavi.jl and adds the position of the first particle to the information panel.\n\n# Creating a System\nsystem = System(\n    state = SecondLawState(\n        pos=[[1 2 3]; [1 2 3]],\n        vel=[[1 1 0]; [-1 0 2]],\n    ),\n    space_cfg=SpaceCfg(\n        wall_type=RigidWalls(),\n        geometry_cfg=RectangleCfg(length=4, height=4),\n    ),\n    dynamic_cfg=LenJonesCfg(sigma=1, epsilon=0.1),\n    int_cfg=IntCfg(dt=0.01),\n)\n\n# Function used to show particle position\n# in the information panel\nfunction get_pos(system, _)\n    pos = system.state.pos[1]\n    pos_formatted = @sprintf(\"(%.3f, %.3f)\", pos[1], pos[2])\n    return [(\"pos[1]\", pos_formatted)]\nend\n\nanim_cfg = AnimationCfg(\n    info_cfg=DefaultInfoUICfg(\n        custom_items=get_pos\n    ),\n    graph_cfg=MainGraphCfg((\n        CircleGraphCfg(), # Render particles as circles\n        NumsGraphCfg(), # Show particle indices\n    ))\n)\n\nanimate(system, anim_cfg)","category":"section"},{"location":"_posts/2025-11-11_rings/#How-to-use-the-Rings-system","page":"How to use the Rings system","title":"How to use the Rings system","text":"To use the rings system we need to do the following:\n\nDefine dynamical parameters (interaction parameters such as springs stiffness, activity, etc)\nCreate initial condition (positions and polarizations at initial time)\nDefine space configuration (geometry plus boundary behavior)\nSet numerical parameters (time step size, box size for space partitioning, etc)\n\nlet's address one item at a time.","category":"section"},{"location":"_posts/2025-11-11_rings/#1.-Dynamical-parameters","page":"How to use the Rings system","title":"1. Dynamical parameters","text":"We need to configure the interaction potential between particles and the rings properties, in code this looks like this\n\n# Using the module which contains things related\n# to model configurations\nusing Mavi.Rings.Configs\n\n# Creating an object which contains the parameters related\n# to how particles interact. Specifically, we are\n# creating configurations for a harmonic truncated\n# potential.\ninteraction_cfg = HarmTruncCfg(\n    k_rep=20,\n    k_atr=4,\n    dist_eq=1,\n    dist_max=1 + 0.2,\n)\n\n# Creating the object which contains all\n# dynamical configurations\ndynamic_cfg = RingsCfg(\n    p0=3.5,\n    relax_time=1,\n    vo=1,\n    mobility=1,\n    rot_diff=0.05,\n    k_area=1,\n    k_spring=20,\n    l_spring=1,\n    interaction_finder=interaction_cfg,\n)","category":"section"},{"location":"_posts/2025-11-11_rings/#2.-Initial-Condition","page":"How to use the Rings system","title":"2. Initial Condition","text":"We need to specify the initial particles positions and rings polarizations. These two quantities are used to create an object of type RingsState. We can do that manually or use a helper function such as rectangle_grid, which returns rings positioned in the vertices of a rectangular grid (this function also returns a geometry configuration, which we will talk about in the next section). Use example\n\nusing Mavi.Rings.States\nusing Mavi.Rings.InitStates\n\nnum_cols = 3\nnum_rows = 3\n\nrings_pos, geometry_cfg = rectangular_grid(\n    num_cols=num_cols, # How many columns the grid has\n    num_rows=num_rows, # How many rows the grid has\n    num_particles=10, # How many particles each ring has\n    p_radius=1, # Rings particles radius\n    pad_x=0.1, pad_y=0.1, # Spacing between rings in the x and y direction\n)\n\n# Creating the initial state\nnum_rings = num_cols * num_rows\nstate = RingsState(\n    rings_pos=rings_pos,\n    pol=random_pol(num_rings), # creating random polarizations using the helper function `random_pol` \n)\n\n\nOBS: Usually, the particle radius is determined by the dynamical configuration, so one can use the function Configs.particle_radius (which is available if we are using Mavi.Rings.Configs) to get the particle radius. Ex: pr = Configs.particle_radius(dynamic_cfg) where dynamic_cfg is the object we created in section 1","category":"section"},{"location":"_posts/2025-11-11_rings/#3.-Space-configuration","page":"How to use the Rings system","title":"3. Space configuration","text":"A space configuration in the combination of a geometry configuration and boundary behavior, for instance, if we want a rectangular geometry with periodic boundaries, we would do the following\n\nusing Mavi.Rings.Configs\n\ngeometry_cfg = Configs.RectangleCfg(\n    length=10,\n    height=10,\n)\n\nwall_type = Configs.PeriodicWalls()\n\nspace_cfg = Configs.SpaceCfg(\n    geometry_cfg=geometry_cfg,\n    wall_type=wall_type,\n)\n\nthe helper function rectangular_grid also returns a rectangular geometry configuration which contains all rings created, so we can use that and only set the boundary behavior\n\n\nrings_pos, geometry_cfg = rectangular_grid(...)\n\nspace_cfg = Configs.SpaceCfg(\n    geometry_cfg=geometry_cfg,\n    wall_type=Configs.PeriodicWalls(),\n)\n\nOBS: three dots in rectangular_grid represent omitted code.","category":"section"},{"location":"_posts/2025-11-11_rings/#4.-Numerical-parameters","page":"How to use the Rings system","title":"4. Numerical parameters","text":"The last thing we need to do is to set numerical parameters (parameters used to perform the numerical integration). The minimum set of parameters we need to configure is the time step\n\nusing Mavi.Rings.Configs\n\nint_cfg = RingsIntCfg(dt=0.01)\n\nbut it is very beneficial to performance the use of a chunks technique (space is partitioned into chunks and interactions are only computed between neighboring chunks). For that, we only need to specify the chunks size, which should be at least the size of the maximum interaction distance (which we can get from dynamic_cfg)\n\nusing Mavi.Rings.Configs\n\ninteraction_cfg = dynamic_cfg.interaction_finder # This is the interaction potential configuration between particles\n\nbbox = space_cfg.geometry_cfg # bbox stands for bounding box\nmax_size = interaction_cfg.dist_max * 1.1 # Setting the size to be a little bit bigger than the maximum interaction distance just to be safe\n\n# Calculating how many chunks we want in each direction\nnum_x_chunks = floor(Int, bbox.length / max_size)\nnum_y_chunks = floor(Int, bbox.height / max_size)\n\np_chunks_cfg = Configs.ChunksCfg(num_x_chunks, num_y_chunks)\n\n# Creating the integration configurations object\nint_cfg = RingsIntCfg(\n    dt=0.01,\n    p_chunks_cfg=p_chunks_cfg,\n)","category":"section"},{"location":"_posts/2025-11-11_rings/#Visualizing-the-simulation","page":"How to use the Rings system","title":"Visualizing the simulation","text":"Now we can put everything together in a RingsSystem and start the simulation with real-time rendering\n\n# Importing everything we need\nusing Mavi.Rings\nusing Mavi.Rings.Configs\nusing Mavi.Rings.InitStates\n\nusing Mavi.Visualization\n\n# =\n# Dynamical Configuration\n# =\n\ninteraction_cfg = HarmTruncCfg(\n    k_rep=20,\n    k_atr=4,\n    dist_eq=1,\n    dist_max=1 + 0.2,\n)\n\ndynamic_cfg = RingsCfg(\n    p0=3.5,\n    relax_time=1,\n    vo=1,\n    mobility=1,\n    rot_diff=0.05,\n    k_area=1,\n    k_spring=20,\n    l_spring=1,\n    interaction_finder=interaction_cfg,\n)\n\n# =\n# Initial State\n# =\n\nnum_cols = 3\nnum_rows = 3\n\nrings_pos, geometry_cfg = rectangular_grid(\n    num_cols=num_cols, # How many columns the grid has\n    num_rows=num_rows, # How many rows the grid has\n    num_particles=10, # How many particles each ring has\n    p_radius=Configs.particle_radius(dynamic_cfg), # Rings particles radius\n    pad_x=0.1, pad_y=0.1, # Spacing between rings in the x and y direction\n)\n\nnum_rings = num_cols * num_rows\nstate = RingsState(\n    rings_pos=rings_pos,\n    pol=random_pol(num_rings), # creating random polarizations using the helper function `random_pol` \n)\n\n# =\n# Space Configuration\n# =\n\nspace_cfg = Configs.SpaceCfg(\n    geometry_cfg=geometry_cfg,\n    wall_type=Configs.PeriodicWalls(),\n)\n\n# =\n# Integration Configuration\n# =\n\ninteraction_cfg = dynamic_cfg.interaction_finder # This is the interaction potential configuration between particles\n\nbbox = space_cfg.geometry_cfg # bbox stands for bounding box\nmax_size = interaction_cfg.dist_max * 1.1 # Setting the size to be a little big bigger than the maximum interaction distance just to be safe\n\n# Calculating how many chunks we want in each direction\nnum_x_chunks = floor(Int, bbox.length / max_size)\nnum_y_chunks = floor(Int, bbox.height / max_size)\n\np_chunks_cfg = Configs.ChunksCfg(num_x_chunks, num_y_chunks)\n\n\n# Creating the integration configurations object\nint_cfg = RingsIntCfg(\n    dt=0.01,\n    p_chunks_cfg=p_chunks_cfg,\n)\n\n# = \n# Creating the System\n# = \n\nsystem = RingsSystem(\n    state=state,\n    space_cfg=space_cfg,\n    dynamic_cfg=dynamic_cfg,\n    int_cfg=Configs.RingsIntCfg(\n        dt=0.01,\n        p_chunks_cfg=Configs.ChunksCfg(num_x_chunks, num_y_chunks),\n        device=Configs.Threaded(),\n    ),\n)\n\n# =\n# Animating the system\n# = \n\nanimate(system)","category":"section"},{"location":"#Mavi.jl","page":"Mavi.jl","title":"Mavi.jl","text":"Mavi is a Particle Dynamics Engine.\n\n(Image: Descrição do GIF)\n\nSee the code that generated this video here: examples/welcome_video.jl\n\nIts goal is to provide a common structure for implementing particle dynamics simulations, allowing users to use default behaviors or create their own as needed.\n\nHere are some Mavi.jl features:\n\nPairwise interactions, such as Lennard-Jones or Harmonic-Truncated potentials.\nSpaces with different geometries, such as rectangular or circular.\nSpaces with different wall types, such as rigid, periodic or dynamic (walls that exert forces on the particles).\nQuantity calculators, such as kinetic and potential energy.\nExperiment system to collect data from simulations.\nVisualization:\nReal-time rendering of simulations using Makie.\nVideo generation of simulations.\nImage generation of simulations.\n\nIn addition to the core Mavi module, there is a module named Mavi.Rings, which implements active rings — a model used in this paper: \"Segregation in Binary Mixture with Differential Contraction among Active Rings\" by Teixeira, E., et al (Physical Review Letters, 2025).\n\nCheck out Mavi.jl examples here. Animations of these examples can be seen here.\nLatest blog post: How to use the Rings system","category":"section"}]
}
